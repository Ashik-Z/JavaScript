let age = window.prompt("Enter your age: ");
age += 1;
console.log(`This is a string, so the increment of age became ${age} years`);

let exp = window.prompt("Enter your experience in years: ");
exp = Number(exp);
exp += 10;
console.log(`This is an integer number, so the increment of exp became ${exp} years`);